#include "main.h"

/**
 * _strlen - returns length of string
 * @s: input parameter
 *
 * Description: uses pointers
 * Return: Always (0)
 */

int _strlen(char *s)
{
	int 1;

	for (i = 0; s[i] != '\0'; ++i)
	{
		continue;
	}

	return (i);
}
